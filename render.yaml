services:
  - type: web
    name: derivation-dash
    env: node
    buildCommand: npm install && npm run build:web
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true

